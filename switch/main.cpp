#include <iostream>
#include<string.h>
#include<stdio.h>
#include<ctype.h>
#include<conio.h>
using namespace std;

/* int main()
{
    int no;
    cout<<"enter the no"<<endl;
    cin>>no;
    switch(no)
    {
        case 1: cout<<"no is one";break;
        case 2: cout<<"no is two";break;
        case 3: cout<<"no is three";break;
        default :cout<<"no is other than 1,2,3";
    }
} */
 void main()

{
    char ch;
    clrscr();
    cout<<"enter the char"<<endl;
    cin>>ch;
    switch(ch)
    {
        case 'jan': cout<<"month is jana";break;
        case 'feb': cout<<"month is j";break;
        case 'mar': cout<<"no is three";break;
        default :cout<<"no is other than 1,2,3";

    }
 }

